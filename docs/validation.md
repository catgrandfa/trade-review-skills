# 验证范围

本文件区分包结构检查、模型行为试用与真实产品安装。结构检查通过不证明交易效果或所有平台端到端兼容。

## 自动检查

当前版本执行以下检查：

- 五个独立 Skill 与统一入口版的名称、frontmatter、入口、模板与资源自包含。
- 共同约定、参考库及文本适配与源文件一致。
- 单技能 ZIP 的目录、文件清单、可解压性与字节一致性。
- 全部六个技能上传 ZIP 不包含独立 `LICENSE` 文件，其余文件与源码逐字节一致；源码目录的许可证保留。
- 发布资产校验和与重复构建的确定性。
- 安装器在临时目录中的安装、重复安装、冲突预检及不覆盖行为。
- 一体版只提供一个根部入口 ZIP，解压后无需独立技能目录也能解析全部本地引用；原技能修改能同步到一体版模块、模板与示例。

```bash
python3 scripts/build.py
python3 scripts/validate.py
python3 -m unittest discover -s tests -v
git diff --check
```

0.3.3 增加随包首次使用示范的检查：独立技能示范修改能同步至一体版模块引用、连续演示和文本适配，FAQ 随所有技能分发。Python 检查器验证本项目使用的扁平 YAML 子集，不是通用 YAML 解析器。临时目录安装不代表真实客户端已发现技能或用户账号已完成导入。

2026-09-15 发布前本地构建、仓库校验、19 项单元测试（包含重复一体包清理回归）及 `git diff --check` 通过。另一个通用技能检查器因当前 Python 缺少 PyYAML 未运行成功，详见[本版检查记录](../evals/results/0.3.3-first-use.md)。

## 行为试用

0.3.3 使用六个隔离虚构案例检查首次使用和短答：一体版复盘与 FAQ 各一案，四个独立版各一案。本轮未发现事实、路由或采用状态错误，但部分回答仍有可压缩的补充；没有旧版对照，不声称已量化改善。完整输出、包摘要与未覆盖项见[本版独立试用](../evals/results/0.3.3-first-use.md)。

提供三组虚构输入：[基础场景](../evals/cases.json)、[作者经验场景](../evals/author-experience-cases.json)与[统一入口场景](../evals/suite-cases.json)。评估方法见[试用说明](../evals/README.md)。

0.3.0 对统一入口版完成三个独立虚构案例，覆盖五个模块的任务选择及两组跨模块衔接；本轮复核未发现阻断问题。完整输出、文件摘要、逐案判断与未覆盖项见[独立试用记录](../evals/results/0.3.0-suite.md)。五个独立版和聊天适配未在本轮重新做全量行为试用，不能从三案外推全部场景。

行为试用不能证明长期交易收益、稳定识别心理状态或所有模型表现一致，也不替代真实平台安装测试。

## 产品验证

平台入口的文档依据见[兼容矩阵](compatibility.md)。2026-09-14 在 Edge 的 SkillHub 发布页验证了 v0.3.0 `trade-review-suite.zip`：页面识别 23 个文件、入口和描述，见[实测记录](skillhub-upload-check.md)。这仅证明该入口当时成功解析所选 ZIP，尚未提交审核或验证安装运行。

0.3.1 后续在 WorkBuddy 5.5.6 完成用户级 GitHub 目录安装、已安装列表发现和三个新任务调用；ChatGPT 完成 GitHub Release 根部入口 ZIP 的账号技能导入，并在新对话中调用。实际回答存在来源编号改写和候选建议采用范围表述过宽的问题，不能报告全部行为通过。安装路径、原句和边界见[产品实测记录](product-trials-0.3.1.md)。

ClawHub、Claude、Codex、Claude Code 和 Cursor 仍未在本轮做账号导入或产品内运行测试。普通文本适配不应被报告为原生安装成功。

0.3.1 移除了上传 ZIP 的独立许可证文件，该版本一体版为 22 个文件；未在 SkillHub 重测。运行指令与 0.3.0 相同，后续产品试用单独记录，不覆盖先前的独立代理试用。

0.3.3 的唯一一体版上传 ZIP 为 29 个文件，新增 FAQ、五个首次使用示范及自动汇成的连续演示。GitHub Release 发布与产品验证分开：本版未做 SkillHub 评分复测、产品上传或真实账号安装测试。
